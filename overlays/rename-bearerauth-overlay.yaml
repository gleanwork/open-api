overlay: 1.0.0
x-speakeasy-jsonpath: rfc9535
info:
  title: Rename security scheme BearerAuth → APIToken
  version: 0.1.2

actions:
  - target: "$.components.securitySchemes"
    action: update
    value:
      APIToken:
        type: http
        scheme: bearer

  - target: "$.components.securitySchemes.BearerAuth"
    remove: true

  - target: "$.security"
    action: update
    value:
      - APIToken: []
